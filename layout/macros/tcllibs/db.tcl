global env

namespace eval db:: {
        proc setPrefValue { args } {
        # stub
        }
        proc getProcessInfo { args } {
        puts "db::getProcessInfo not supported"
        }
        proc listAttrs { args } {
        puts "db::listAttrs not supported"
        }
        #proc getCurrentRef { args } {
        #puts "db::getCurrentRef not supported"
        #return 0
        #}
        #proc getParamValue { args } {
        #return [iPDK_getParameterValue args]
        #}
}
